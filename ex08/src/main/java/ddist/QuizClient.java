package ddist;

public class QuizClient {
    private final int _port;

    /**
     * @param port the port on which a server is running
     */
    public QuizClient(int port) {
        _port = port;
    }

    public void run() {
        // Create socket and connect to server

        // Create input and output streams on the socket

        // Create input and ouptput streams for the command line

        // Start thread for receiving QAs
        //      Wait for incoming QA
        //      Print it to STDOUT
        //      Print the question input prompt
        //      Repeat

        // Print the question input prompt
        // Wait for input on the command line
        //      Retrieve question
        //      Print the question input prompt
        //      Create QA
        //      Send it to the server
    }
}
